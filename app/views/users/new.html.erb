<% content_for(:title, t('.title')) %>
<div class="flex justify-center min-h-screen p-5">
  <div class="max-w-xl w-full py-5 bg-white rounded-lg shadow-lg">
    <h1 class="text-4xl font-bold mb-8 text-center">ユーザー登録</h1>
    <%= form_with model: @user do |f| %>
      <div class="flex items-center justify-center mb-8">
        <%= link_to auth_at_provider_path(provider: :google), method: :post, class: "px-6 py-3 border flex gap-2 border-blue-200 dark:border-blue-300 rounded-lg text-blue-700 dark:text-blue-200 hover:border-blue-400 hover:text-blue-800 hover:shadow-outline transition duration-150 text-lg" do %>
          <img class="w-8 h-8" src="https://www.svgrepo.com/show/475656/google-color.svg" loading="lazy" alt="google logo">
          <span class="text-xl">Googleで新規登録<br class="md:hidden">またはログイン</span>
        <% end %>
      </div>

      <div class="flex items-center justify-center mb-2">
        <div class="flex-grow border-t border-gray-300"></div>
        <span class="flex-shrink mx-4 text-lg">または</span>
        <div class="flex-grow border-t border-gray-300"></div>
      </div>

      <%= render 'shared/error_messages', object: f.object %>

      <div class="card-body mx-auto">
        <div class="form-control">
          <%= f.label :email, 'メールアドレス', class: "label-text mb-1" %>
          <%= f.email_field :email, placeholder: "example@congrawa.com", class: "input input-bordered text-lg", required: true, autocomplete: "email" %>
        </div>
        <div class="form-control mt-2">
          <%= f.label :password, 'パスワード　※8文字以上で入力してください', class: "label-text mb-1" %>
          <%= f.password_field :password, class: "input input-bordered text-lg", required: true, autocomplete: "password" %>
        </div>
        <div class="form-control mt-4 lg:mt-6">
          <%= f.submit "登録", class: "btn btn-primary w-full" %>
        </div>
      </div>
    <% end %>

    <div class='text-center text-base text-gray-600'>
      <%= link_to 'ログインページへ', login_path, class: "text-blue-500 hover:text-blue-600 text-lg" %>
    </div>
  </div>
</div>
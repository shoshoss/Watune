<%= turbo_stream.prepend "replies_for_post_#{@post.id}" do %>
  <%= render @reply %>
<% end %>

<%= turbo_stream.replace "new_reply_#{@post.id}" do %>
  <%= render 'replies/form', post: @post, reply: @post.replies.build %>
<% end %>

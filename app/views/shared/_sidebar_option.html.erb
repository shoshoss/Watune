<div class="sidebarOption flex items-center space-x-4 p-3 hover:bg-blue-200 rounded-full transition-colors duration-300 <%= 'bg-blue-100' if local_assigns[:active] %>">
  <i class="fas <%= local_assigns[:icon] %> text-2xl"></i>
  <h2 class="text-lg font-bold flex-grow"><%= local_assigns[:text] %></h2>
</div>

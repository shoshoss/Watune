<!-- モーダルを開くボタン -->
<button class="btn btn-primary" onclick="document.getElementById('loginModal').showModal()">ログイン</button>

<!-- モーダルのHTML -->
<dialog id="loginModal" class="modal">
  <div class="modal-box">
    <h1 class="text-4xl font-bold mb-8 text-center">ログイン</h1>
    <%= form_with url: login_path do |f| %>
      <!-- Googleでログインのボタン -->
      <div class="flex items-center justify-center mb-8">
        <%= link_to auth_at_provider_path(provider: :google), method: :post, class: "px-6 py-3 border flex gap-2 border-blue-200 dark:border-blue-300 rounded-lg text-blue-700 dark:text-blue-200 hover:border-blue-400 hover:text-blue-800 hover:shadow-outline transition duration-150 text-lg" do %>
          <img src="https://www.svgrepo.com/show/475656/google-color.svg" alt="Google logo" class="w-8 h-8">
          <span class="text-xl">Googleでログイン</span>
        <% end %>
      </div>

      <!-- または -->
      <div class="flex items-center justify-center mb-2">
        <div class="flex-grow border-t border-gray-300"></div>
        <span class="flex-shrink mx-4 text-lg">または</span>
        <div class="flex-grow border-t border-gray-300"></div>
      </div>

      <!-- メールアドレス入力 -->
      <div class="form-control mt-2">
        <%= f.label :email, 'メールアドレス', class: "label-text mb-1" %>
        <%= f.email_field :email, placeholder: "example@congrawa.com", class: "input input-bordered text-lg", required: true, autocomplete: "email" %>
      </div>

      <!-- パスワード入力 -->
      <div class="form-control mt-2">
        <%= f.label :password, 'パスワード', class: "label-text mb-1" %>
        <%= f.password_field :password, class: "input input-bordered text-lg", required: true, autocomplete: "current-password" %>
      </div>

      <!-- ログインボタン -->
      <div class="form-control mt-4 lg:mt-6">
        <%= f.submit "ログイン", class: "btn btn-primary w-full" %>
      </div>
    <% end %>

    <!-- パスワードリセットと新規登録のリンク -->
    <div class="text-center text-base text-gray-600 mt-4">
      <%= link_to t('.password_forget'), new_password_reset_path, class: "link link-hover text-lg" %>
      <%= link_to t('.to_register_page'), new_user_path, class: "link link-hover text-lg mt-2" %>
    </div>

    <!-- モーダルを閉じるボタン -->
    <div class="modal-action">
      <button class="btn" onclick="document.getElementById('loginModal').close()">閉じる</button>
    </div>
  </div>
</dialog>

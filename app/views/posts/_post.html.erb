<% if post.is_a?(Repost) %>
  <% repost = post %>
  <%= render partial: 'posts/repost', locals: { post: repost.post } %>
<% else %>
  <%= render partial: 'posts/post_content', locals: { post: post } %>
<% end %>

<!-- 投稿内容 -->
  <div class="mb-2 text-[15px] overflow-y-auto max-h-[calc(1.5rem*10)] md:max-h-[calc(1.5rem*15)] whitespace-normal break-words break-all">
    <% post.body.split(/\r\n|\r|\n/).each do |line| %>
      <% if line.empty? %>
        <div class="text-xxs">
          <br>
        </div>
      <% else %>
        <div class="text-[15px]">
          <%= line %><br>
        </div>
      <% end %>
    <% end %>
  </div>
  
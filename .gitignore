/certs

# バンドラーの設定フォルダ
/.bundle

# システムファイル
/vendor

# 環境設定ファイル
/.env*
!/.env*.erb

# ログファイルと一時ファイル
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# PIDファイル（プロセスIDを保存するファイル）だけどディレクトリは保持
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# アップロードされたファイル（開発環境では無視）
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

# アセットパイプラインのキャッシュ
/public/assets
/public/uploads

# マスターキー
/config/master.key

# Node.jsのモジュール
/node_modules

# Yarnのエラーログ
yarn-error.log

# Precompiled asset files
/app/assets/builds
!/app/assets/builds/.keep

# 必要に応じて追加した無視ファイル（これは例として追加されたものです）
/config/initializers/secret_token.rb

# 依存関係を管理するためのフォルダ
/vendor/bundle
/node_modules
/yarn-error.log

# 環境固有の設定ファイル
config/settings.local.yml
config/settings/*.local.yml
config/environments/*.local.yml
